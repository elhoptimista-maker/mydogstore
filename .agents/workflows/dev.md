---
description: Iniciar el servidor de desarrollo de Mydog Store
---
// turbo-all
1. Cargar NVM e iniciar el servidor de desarrollo en el puerto 9002:
   export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && npm run dev
